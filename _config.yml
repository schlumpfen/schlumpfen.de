# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.

title: schlumpfen.de
author: Stefan Roschewitz
email: stefan@roschewitz.de
description: >- # this means to ignore newlines until "baseurl:"
  Web Seite von Stefan Roschewitz. Auf dieser Seite versuche
  ich verschiedene Dinge zu posten, die mir wichtig sind bzw. einfallen.
baseurl: "" # the subpath of your site, e.g. /blog
url: "https://schlumpfen.de" # the base hostname & protocol for your site, e.g. http://example.com

favicon: "logo.png" # name+extension of favicon (which must be put on the root folder)
goat_counter: "schlumpfen" # put your GoatCounter name if you want to use GoatCounter analytics

theme: no-style-please

theme_config:
  appearance: "auto" # can be "light", "dark" or "auto"
  back_home_text: "<- Zurück" # customize text for homepage link in post layout
  date_format: "%d.%m.%Y" # customize how date is formatted
  show_description: false # show blog description in home page

sass:
  style: compressed

# Build settings
plugins:
  - jekyll-feed
  - jekyll-seo-tag
